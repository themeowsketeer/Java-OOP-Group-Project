spring:
  datasource:
    url: jdbc:postgresql://localhost:1234/Library
    username: postgres
    password: Dlwlrma1605
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#    generate-ddl: true